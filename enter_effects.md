## enter_effects — 进入效果命名规范

格式：`效果名[,参数]`，使用英文小写或中文，参数用逗号分隔。

### 有方向 / 参数的效果

| 格式示例 | 中文名 | 参数说明 | 中文参数说明 |
|----------|--------|----------|----------|
| `blinds,horizontal` | 百叶窗 | `horizontal`（默认）/ `vertical` | 横向(默认) / 纵向 |
| `box,in` | 盒状 | `in`（默认）/ `out` | 向内(默认) / 向外 |
| `checkerboard,across` | 棋盘 | `across`（默认）/ `down` | 交叉(默认) / 向下 |
| `circle,in` | 圆形扩展 | `in`（默认）/ `out` | 向内(默认) / 向外 |
| `diamond,in` | 菱形 | `in`（默认）/ `out` | 向内(默认) / 向外 |
| `fly,left` | 飞入 | `left` / `right` / `up` / `down` / `bottom-left` / `bottom-right` / `top-left` / `top-right` | 向左 / 向右 / 向上 / 向下 / 左下 / 右下 / 左上 / 右上 |
| `plus,in` | 十字形扩展 | `in`（默认）/ `out` | 向内(默认) / 向外 |
| `random-bars,horizontal` | 随机线条 | `horizontal`（默认）/ `vertical` | 横向(默认) / 纵向 |
| `split,h-out` | 劈裂 | `h-out`（默认）/ `h-in` / `v-out` / `v-in` |  横外(默认) / 横内 / 纵外 / 纵内 |
| `stairs,left-down` | 阶梯状 | `left-down`（默认）/ `right-down` / `left-up` / `right-up` | 左下(默认) / 右下 / 左上 / 右上 |
| `stretch,across` | 伸展 | `across`（默认）/ `up` / `down` | 交叉(默认) / 向上 / 向下 |
| `strips,left-down` | 切入 | `left-down`（默认）/ `right-down` / `left-up` / `right-up` | 左下(默认) / 右下 / 左上 / 右上 |
| `wheel,1` | 轮子 | `1`（默认）/ `2` / `3` / `4` / `8`（轮辐数） | 1(默认) / 2 / 3 / 4 / 8 |
| `wipe,left` | 擦除 | `left`（默认）/ `right` / `up` / `down` | 向左(默认) / 向右 / 向上 / 向下 |
| `zoom,in` | 缩放 | `in`（默认）/ `out` | 向内(默认) / 向外 |
| `zoom-basic,in` | 基本缩放 | `in`（默认）/ `out` | 向内(默认) / 向外 |

### 无参数的效果

| 格式 | 中文名 |
|------|--------|
| `appear` | 出现 |
| `bounce` | 弹跳 |
| `center-revolve` | 中心旋转 |
| `compress` | 压缩 |
| `credits` | 字幕式 |
| `curve-up` | 曲线向上 |
| `dissolve` | 向内溶解 |
| `drop` | 掉落 |
| `expand` | 展开 |
| `fade` | 淡化 |
| `flip` | 翻转式由远及近 |
| `float` | 浮动 |
| `float-down` | 下浮 |
| `float-up` | 上浮 |
| `pinwheel` | 飞旋 |
| `rise` | 升起 |
| `somersault` | 空翻 |
| `spin` | 旋转 |
| `spin-basic` | 基本旋转 |
| `spiral` | 螺旋飞入 |
| `swivel` | 回旋 |
| `wedge` | 楔入 |
| `whip` | 挥鞭式 |
| `windmill` | 玩具风车 |
